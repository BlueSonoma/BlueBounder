import { Colors } from '../../../types';

export const initialState = {
  chemImagePath: '',
  scale: 50,
  sigma: 0.1,
  minSize: 10,
  outlineColor: Colors.Black,
  label: true,
  uniformLabel: true,
  overlay: true,
  bandImagePath: '',
  labelColor: Colors.Yellow,
  labelOpacity: 0.4,
  outputPath: '',
  outputFilename: '',
};
